# Domain Docs

This repo uses a single-context domain documentation layout.

Before exploring domain-sensitive work, read:

- `CONTEXT.md` at the repo root, if it exists.
- `docs/adr/`, if it exists, for architectural decisions relevant to the area being changed.

If these files do not exist, proceed silently. Domain docs are created lazily by the domain-modeling workflows when terms or decisions are resolved.
