Project conventions from AGENTS.md: preserve Thai text and UTF-8 encoding exactly; avoid mojibake; use `apply_patch` for edits; when using PowerShell file reads specify `-Encoding UTF8`. Priorities: correctness > security > performance > maintainability. No `any`; use explicit types/return types. Reuse existing logic, keep style-only changes from modifying business logic. Prefer small focused functions, early returns, immutability, SRP/DRY/KISS. Validate inputs at boundaries, avoid exposing internals, no hardcoded secrets, parameterized DB access only. Architecture flow: UI -> Hooks -> Services -> Data Layer. Use migrations for production schema changes.